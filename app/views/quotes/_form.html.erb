<div class="container">
  <%= form_with(model: quote) do |form| %>
    <% if quote.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

        <ul>
          <% quote.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="mb-3">
      <%= form.label :saying, class: "form-label" %>
      <%= form.text_field :saying, class: "form-control" %>
    </div>

    <div class="mb-3">
      <%= form.label :said_by, class: "form-label" %>
      <%= form.text_field :said_by, class: "form-control" %>
    </div>

    <div class="mt-3">
      <%= form.submit "Create Quote", class: "btn btn-success" %>
      <%= link_to "Cancel", quotes_path, class: "btn btn-dark" %>
    </div>
  <% end %>
</div>

<footer class="container">
  <p class="bg-dark text-white text-center footer mt-5"> &copy All rights reserved - WEB 3200 - 2024</p>
  </footer>